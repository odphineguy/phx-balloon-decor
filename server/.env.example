# The dev server loads the repo-root .env first (TENANT_ID, OPENAI_API_KEY,
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY — see ../.env.example).
# This file is for dev-only overrides.

# Server port (optional, defaults to 3001)
PORT=3001
